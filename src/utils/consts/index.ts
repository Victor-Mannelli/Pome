import { parseCookies } from 'nookies';

export const token = parseCookies(null).token;

export const monthNames = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];
